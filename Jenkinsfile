pipeline {
         agent any
         stages {
                 stage('One') {
                 steps {
                     echo 'We are in stage One'
						}
                 }
				 stage('Two') {
                 steps {
                     echo 'Hi, We are in stage Two'
						}
                 }
                 
				}
}
